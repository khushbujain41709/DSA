// If we need sum of all elements in range (3,7) both inclusive in an array. We first make a prefix sum or running Sum array then subtract index 7 and index 2 element of the prefix sum array.
// sum(a,b) = prefix(b) - prefix(a-1). 