// Minimum Spanning Tree (MST) is a subset of the edges of a connected, edge-weighted undirected graph that connects all the vertices together, without any cycles and with the minimum possible total edge weight.
// It can be solved using Prim's algorithm or Kruskal's algorithm.
// It contains N-1 edges where N is the number of vertices in the graph and N vertices. It is a tree that spans all the vertices in the graph and has the minimum total edge weight among all possible spanning trees of the graph.
#include<bits/stdc++.h>
using namespace std;
int main(){
    
    return 0;
}