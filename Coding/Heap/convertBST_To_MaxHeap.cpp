// We get a decreasing order sorted array when we preform reverse inorder traversal.
// Now fill this in level order(level by level) in a BST
#include<iostream>
#include<vector>
using namespace std;
class TreeNode{
    public:
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int data){
        val = data;
        left = NULL;
        right = NULL;
    }
};
int main(){
    
    return 0;
}