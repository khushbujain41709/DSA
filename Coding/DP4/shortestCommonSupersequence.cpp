// Leetcode 1092
#include<iostream>
#include<vector>
using namespace std;
// s1 = adbecf and s2 = paqbrc
// lcs is abc 
// ans is padqbrecf
// abcdefpqr is wrong because it is a subsequence so we have to maintain order.
// length of shortest common supersequence is len(lcs) + len(s1) - len(lcs) + len(s2) - len(lcs)
int main(){
    
    return 0;
}