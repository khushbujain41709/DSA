#include<iostream>
using namespace std;
int main(){
    float x , y;
    // float* p1 = &x, p2 = y; // wrong way to declare two pointers . It will consider p1 as pointer as *p1 but p2 as an integer.
    // float* p1 = &x, p2 = &y;   wrong way to declare two pointers .
    return 0;
} 